Set up Tasks:
  - update todoID of a task if todo is persisted
  - remove todo.data.tasks property
Check what happens when operating on a resource etc fails

Today, Upcoming etc links (data-fns)

LocalStorage
